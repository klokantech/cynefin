document.addEventListener("DOMContentLoaded", function() {
  
    new Forms();   

    

    // Height for box scroll
 //    var windowHeight = Math.max(document.documentElement.clientHeight, window.innerHeight || 0);
 //    var paddingTop = 130; // header height
 //    var offset = windowHeight - paddingTop;

 //    if(document.querySelectorAll('#county-scroll').length) {	
	//     document.getElementById('county-scroll').style.height = offset - 92 + "px"; // - box-title height
	// }
	// if(document.querySelectorAll('#counties-scroll').length) {	
	//     document.getElementById('counties-scroll').style.height = offset - 77 + "px"; // - box-title height
	// }


   
});
